import React from 'react';
import { Section, Empty } from './shared/ui';
const PortalAdmin: React.FC = () => (
  <Section title="Vendor Portal"><Empty msg="Coming up." /></Section>
);
export default PortalAdmin;
