name: Deploy to VPS

on:
  push:
    branches: [main]
    paths:
      - 'landing-page/**'
      - 'server/**'
      - 'deploy/nginx.conf'
      - '.github/workflows/deploy-vps.yml'
  workflow_dispatch:
    inputs:
      force_deploy:
        description: 'Force full re-deploy even if no changes'
        required: false
        default: 'false'
        type: boolean

jobs:
  deploy:
    name: Deploy landing page to VPS
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      # ── 1. Checkout ──────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── 2. SSH agent setup ───────────────────────────
      - name: Set up SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.VPS_SSH_KEY }}

      # ── 3. Add VPS to known hosts ────────────────────
      - name: Trust VPS host key
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H "$VPS_HOST" >> ~/.ssh/known_hosts

      # ── 4. Deploy landing page via rsync ─────────────
      - name: Sync landing page to VPS
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
        run: |
          rsync -avz --delete \
            --exclude='.git' \
            --exclude='CNAME' \
            landing-page/ \
            "${VPS_USER}@${VPS_HOST}:/var/www/accounting.rmpgutah.us/"

      # ── 5. Set correct file permissions on VPS ───────
      - name: Fix permissions on VPS
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
        run: |
          ssh "${VPS_USER}@${VPS_HOST}" \
            "find /var/www/accounting.rmpgutah.us -type d -exec chmod 755 {} \; && \
             find /var/www/accounting.rmpgutah.us -type f -exec chmod 644 {} \; && \
             echo 'Permissions OK'"

      # ── 5b. Deploy sync server ───────────────────────
      - name: Deploy sync server
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
        run: |
          rsync -az --delete \
            --exclude='node_modules' \
            --exclude='dist' \
            --exclude='.env' \
            --exclude='data' \
            --exclude='portal/dist' \
            --exclude='portal/node_modules' \
            server/ \
            "$VPS_USER@$VPS_HOST:/opt/bap-server/"
          ssh "$VPS_USER@$VPS_HOST" \
            'cd /opt/bap-server && npm ci --omit=dev && npm run build && cd portal && npm ci && npm run build && cd .. && (pm2 restart bap-server || pm2 start dist/index.js --name bap-server) && pm2 save && echo "Sync server deployed"'

      # ── 6. Reload Nginx gracefully ───────────────────
      - name: Reload Nginx
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
        run: |
          ssh "${VPS_USER}@${VPS_HOST}" \
            "sudo nginx -t && sudo nginx -s reload && echo 'Nginx reloaded'"

      # ── 7. Health check ──────────────────────────────
      - name: Verify site is live
        run: |
          STATUS=$(curl -sSo /dev/null -w "%{http_code}" \
            --max-time 10 \
            --retry 3 \
            --retry-delay 3 \
            "https://accounting.rmpgutah.us/")
          echo "HTTP status: $STATUS"
          if [ "$STATUS" != "200" ]; then
            echo "::error::Site health check failed — HTTP $STATUS"
            exit 1
          fi
          echo "✓ Site is live at https://accounting.rmpgutah.us"
          SERVER_STATUS=$(curl -sSo /dev/null -w "%{http_code}" \
            --max-time 10 \
            "https://accounting.rmpgutah.us/api/health" || echo "000")
          echo "Sync server health: $SERVER_STATUS"
          if [ "$SERVER_STATUS" != "200" ]; then
            echo "::warning::Sync server health check failed — HTTP $SERVER_STATUS (may not be deployed yet)"
          else
            echo "✓ Sync server is live"
          fi

      # ── 8. Summary ───────────────────────────────────
      - name: Deployment summary
        if: success()
        env:
          COMMIT_SHA: ${{ github.sha }}
          REF_NAME: ${{ github.ref_name }}
          ACTOR: ${{ github.actor }}
        run: |
          echo "## ✅ Deployment Successful" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Field | Value |" >> "$GITHUB_STEP_SUMMARY"
          echo "|-------|-------|" >> "$GITHUB_STEP_SUMMARY"
          echo "| Site | https://accounting.rmpgutah.us |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Commit | \`${COMMIT_SHA:0:8}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Branch | \`${REF_NAME}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Triggered by | ${ACTOR} |" >> "$GITHUB_STEP_SUMMARY"
