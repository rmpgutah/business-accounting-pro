appId: us.rmpgutah.accounting
productName: Business Accounting Pro
copyright: Copyright 2026 RMPG
# electron-updater reads latest-mac.yml / latest.yml from GitHub Releases
publish:
  provider: github
  owner: rmpgutah
  repo: business-accounting-pro
directories:
  output: release
  buildResources: build
files:
  - dist/**/*
  - node_modules/better-sqlite3/**/*
  - node_modules/bindings/**/*
  - node_modules/file-uri-to-path/**/*
  - package.json
extraResources:
  - from: src/main/database/schema.sql
    to: schema.sql
asar: true
asarUnpack:
  - node_modules/better-sqlite3/**
mac:
  category: public.app-category.finance
  icon: build/icon.icns
  darkModeSupport: true
  target:
    - target: dmg
      arch:
        - arm64
        - x64
    - target: zip
      arch:
        - arm64
        - x64
  # When CSC_IDENTITY_AUTO_DISCOVERY=false, electron-builder skips signing.
  # The workflow's afterBuild step handles consistent ad-hoc codesigning.
  hardenedRuntime: false
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  artifactName: BusinessAccountingPro-${version}-mac-${arch}.${ext}
dmg:
  title: Business Accounting Pro
  artifactName: BusinessAccountingPro-${version}-mac-${arch}.${ext}
win:
  icon: build/icon.ico
  target:
    - target: nsis
      arch:
        - x64
  artifactName: BusinessAccountingPro-Setup-${version}.${ext}
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  shortcutName: Business Accounting Pro
  createDesktopShortcut: true
linux:
  icon: build/icon.png
  target:
    - AppImage
    - deb
  category: Finance
